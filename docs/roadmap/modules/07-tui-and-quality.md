# 模块规划：TUI 与质量保障

## 模块范围

覆盖以下内容：

- `apps/tui` 预留
- 端到端验证
- 关键用户路径质量控制

目标是保证未来接入 TUI 时不需要返工核心链路，并补上当前缺失的验证体系。

## 当前状态

- [x] API 契约基本保持展示层无关
- [ ] `apps/tui` 初始骨架未建立
- [ ] TUI 首版需求清单未形成
- [ ] Web 关键用户路径 E2E 未建立

## 当前重点

### P0

- [ ] 记录 TUI 首版需求清单
- [ ] 明确 TUI 对 API 的最小依赖集合
- [ ] 增加 Web 关键路径端到端验证

### P1

- [ ] 评估 CLI / TUI / API 的协作边界
- [ ] 补语言切换与主题切换的回归验证

## 风险

- 没有 E2E，Web 回归只能靠手工发现
- 没有 TUI 需求清单，后续很容易把“展示层无关”停留在口头上

## 完成标准

- [ ] 后续新增 `apps/tui` 时不需要修改核心分析逻辑
- [ ] Web 关键用户路径具备基础 E2E
- [ ] TUI 首版范围与接口依赖清单明确
