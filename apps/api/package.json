{
  "name": "@code-dance/api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node --import tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "@code-dance/analyzer": "workspace:*",
    "@code-dance/contracts": "workspace:*",
    "@code-dance/domain": "workspace:*",
    "@code-dance/git": "workspace:*",
    "@code-dance/storage": "workspace:*",
    "@fastify/cors": "^10.0.1",
    "fastify": "^5.1.0"
  },
  "devDependencies": {}
}
